<footer>
    <link rel="stylesheet" href="/css/styles.css">
    <% if(!isAuth) { %>
        <p>Not logged in</p>
        <% } else { %>
            <p>
                logged in as : <%= username %>
            </p>
            <% } %>
            <% if(isAuth) { %>
                <%- include('../partials/cart.ejs') %> 
            <% } %>
</footer>